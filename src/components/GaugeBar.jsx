import {formatNumber} from '../utils/calc';export default function GaugeBar({label,value,max,unit='',decimals=1,sub}){const p=max>0?Math.min(100,value/max*100):0;return <div><div className="flex items-baseline justify-between mb-1"><span className="field-label" style={{margin:0}}>{label}</span><span style={{fontWeight:800,fontSize:12}}>{formatNumber(value,decimals)} <span className="muted">{unit}</span>{sub&&<span className="muted"> {sub}</span>}</span></div><div style={{height:9,borderRadius:99,background:'var(--surface2)',overflow:'hidden',border:'1px solid var(--line)'}}><div style={{height:'100%',width:`${p}%`,background:'linear-gradient(90deg,#5146e5,#7a6ff2)',borderRadius:99,transition:'width .3s'}}/></div></div>}
